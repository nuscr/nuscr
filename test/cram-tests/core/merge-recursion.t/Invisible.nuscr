// An invisible loop does not supply an observable choice to B.
global protocol Invisible(role A, role B, role C) {
  choice at C {
    One() from C to A;
    rec X {
      Tick() from A to C;
      continue X;
    }
  } or {
    Two() from C to A;
    Tock() from A to B;
  }
}
