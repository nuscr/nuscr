(*# RefinementTypes, GuardedUniqueness #*)

global protocol Dup(role A, role B) {
  PrevMsg(y: int{y = 0}) from A to B;
  choice at A {
    Msg(x: int{x = y}) from A to B;
  } or {
    Msg(x: int{x <> 0}) from A to B;
  }
}
