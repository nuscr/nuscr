(*# RefinementTypes, GuardedUniqueness #*)

global protocol RunningSum(role C, role S) {
  rec loop [total<C, S>: (total: int{total < 100}) = 0] {
    choice at C {
      bye(x:int{x < 0}) from C to S;
      continue loop [total + x];
    } or {
      bye(x:int{x > 0}) from C to S;
    }
  }
}
