let solver_show_queries = ref false

let nested_protocol_enabled = ref false

let refinement_type_enabled = ref false

let reset () =
  solver_show_queries := false ;
  nested_protocol_enabled := false ;
  refinement_type_enabled := false
