(library
  (flags (:standard -w +A-39-4-42))
  (name nuscrlib)
  (preprocess
    (pps ppx_deriving.show)))


(ocamllex
  (modules lexer))

(menhir
  (modules parser)
  (flags ("--explain" "-la" "2")))
